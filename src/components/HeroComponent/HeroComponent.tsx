import React from 'react';
import styles from 'HeroComponent.module.scss';

const HeroComponent = () => {
  return (
    <div>
      <h2 className={styles.title}>Project managment app</h2>
    </div>
  );
};

export default HeroComponent;
